Captured starting run for scenario 'migration-legacy-models'.

Command: python scripts/workshop.py verify migration-legacy-models
Result: exit 3 (acceptance not met)
Summary: 0/1 acceptance checks passed

Twenty-one model and adapter checks run. The published model, REST, MCP, and
batch contracts pass in the starting state:

- aliases, key order, defaults, and runtime types
- dot-decimal machine values
- aware-UTC normalization and published timestamp text
- accepted and rejected input boundaries
- REST envelope and nested shape
- MCP text/structured equivalence
- deterministic batch publication

MigrationCompletedTest fails because the models still import `pydantic.v1` and
runtime model classes still come from the compatibility layer. This is synthetic
fail-before evidence.
