Captured starting run for scenario 'capstone-transfer'.

Command: python scripts/workshop.py verify capstone-transfer
Result: exit 3 (acceptance not met)
Summary: 0/1 acceptance checks passed

The registry, naive-time rejection, future exclusion, insufficient-sample
behaviour, and unknown-policy error pass.

The starting failures are:

- the freshness selector includes a 45-minute-old reading outside the 15-minute
  window;
- the policy therefore reports four samples and average Decimal('124.50')
  instead of three samples and Decimal('106.00');
- an absolute deviation exactly equal to the threshold returns no
  recommendation; and
- the machine payload converts average and deviation values to binary floats
  instead of dot-decimal strings.

This captured output is synthetic fail-before evidence for the offline route.
