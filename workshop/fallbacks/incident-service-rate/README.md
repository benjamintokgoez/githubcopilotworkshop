# Offline fallback - incident-service-rate (Lab 2)

Use this copy if scenario staging is unavailable. Copy every file from
`staged_copy/` to a participant-owned directory and remove only the final
`.txt`. Keep the modules together, run the tests there, or use
`captured_acceptance_output.txt` when execution is blocked.

Read `issue.md`, `invariants.md`, and `logs/` first. Record observed facts,
conclusions, assumptions, the cross-module path, bounded plan, review, operations
handover, and uncertainty. Captured output is evidence of the prepared starting
state, not a claim that your repair passed.
