# Acceptance - incident-service-rate

Run the copied staged files with Python's `unittest` runner. The unchanged checks
require the provider offer's rate, rate-time priority, conserved hours, and
positive `Decimal` rates. The captured output shows the expected starting
failure; do not claim a pass unless you executed the checks.
