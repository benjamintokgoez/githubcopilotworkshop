{
  "servers": {
    "mittelwerk-local": {
      "type": "stdio",
      "command": "python",
      "args": ["-m", "mittelwerk.mcp_server.server"],
      "cwd": "${workspaceFolder}",
      "env": {
        "MITTELWERK_ENV": "workshop-simulation"
      },
      "envFile": "${workspaceFolder}/.env",
      "dev": {
        "watch": "mittelwerk/mcp_server/**/*.py"
      },
      "sandboxEnabled": false
    }
  }
}
