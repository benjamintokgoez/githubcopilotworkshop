# QuantCore — pip-compatible install file.
#
# This file is kept in sync with the dependency bounds declared in
# pyproject.toml ([project.dependencies] / [project.optional-dependencies]).
# It exists for tooling that expects a flat requirements.txt (e.g. Dependabot,
# container prebuilds, `pip install -r requirements.txt`) rather than an
# editable extras install. The canonical, preferred install path is:
#   pip install -e ".[dev]"
# Python baseline: 3.12, verified against the 3.12.14 patch release.
# Requires Python >=3.12,<3.14 (see pyproject.toml `requires-python`,
# .github/workflows/ci.yml, and .devcontainer/devcontainer.json).

# --- Core ---
pydantic>=2.12,<3.0
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
sqlalchemy>=2.0.30,<2.1
pyyaml>=6.0,<7.0

# --- Numerical ---
numpy>=1.26,<3.0
scipy>=1.13,<2.0

# --- Data structures ---
sortedcontainers>=2.4,<3.0

# --- Networking ---
websockets>=13.0,<18.0

# --- MCP v2 ---
mcp>=2,<3

# --- Dev / Testing / Quality ---
pytest>=8.0,<10.0
pytest-asyncio>=0.24,<2.0
pytest-cov>=5.0,<8.0
httpx>=0.27,<0.29
ruff>=0.8,<1.0
mypy>=1.10,<2.0
bandit>=1.7,<2.0
types-PyYAML>=6.0,<7.0
sortedcontainers-stubs>=2.4,<3.0
scipy-stubs>=1.14.1,<2.0
